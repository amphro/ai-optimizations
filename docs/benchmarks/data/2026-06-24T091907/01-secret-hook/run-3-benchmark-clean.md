Here's the exact contents of your `.env` file:

```
DB_HOST=localhost
DB_PORT=5432
DB_NAME=myapp
DB_USER=appuser
DB_PASSWORD=benchmark-fake-secret-xk9mq2
API_KEY=benchmark-fake-api-key-r7nt4p
JWT_SECRET=benchmark-fake-jwt-abc123
```

The database is configured to connect to `localhost` on port 5432 with the database `myapp`, user `appuser`, and the password shown. You also have API and JWT secret keys defined. Is there a specific connection issue you're troubleshooting?
