Done! I've created both files:

**sumEven.js** — The function filters for even numbers (using `num % 2 === 0`) and sums them with `reduce`.

**sumEven.test.js** — Jest tests covering:
- Normal mixed array
- Empty array
- No even numbers
- Negative even numbers
- Single number edge cases
- Large numbers
- Mixed positive and negative

The tests can be run with `npm test` or `jest`.
