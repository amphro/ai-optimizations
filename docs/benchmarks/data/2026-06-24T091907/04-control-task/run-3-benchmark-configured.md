Done! I've created:

1. **sumEven.js** — The function filters for even numbers (where `n % 2 === 0`) and returns their sum using `reduce`.
2. **sumEven.test.js** — 8 Jest tests covering:
   - Mixed even/odd numbers
   - Empty arrays
   - All odd numbers
   - All even numbers
   - Zero and negative numbers
   - Single element arrays

All 8 tests pass ✓
