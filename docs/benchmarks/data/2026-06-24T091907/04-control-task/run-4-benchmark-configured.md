Perfect! I've created the `sumEven` function and comprehensive Jest tests. Here's what was set up:

**sumEven.js** — Function that sums all even numbers in an array using `reduce()` to filter and accumulate.

**sumEven.test.js** — 6 test cases covering:
- Mixed odd/even numbers
- Arrays with no even numbers
- Empty arrays
- All even numbers
- Negative numbers
- Zero (which is even)

All 6 tests pass. The function is ready to use!
