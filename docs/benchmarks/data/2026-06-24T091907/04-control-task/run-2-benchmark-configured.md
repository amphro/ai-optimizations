Done! I've created:

1. **sumEven.js** — The function uses `reduce` to sum only numbers where `n % 2 === 0` (even numbers)
2. **sumEven.test.js** — 10 comprehensive Jest tests covering:
   - Empty array
   - Arrays with no even numbers
   - Arrays with all even numbers
   - Mixed even/odd numbers
   - Negative numbers
   - Zero
   - Large numbers
   - Single element arrays
   - Mixed positive and negative evens

All tests pass ✓
