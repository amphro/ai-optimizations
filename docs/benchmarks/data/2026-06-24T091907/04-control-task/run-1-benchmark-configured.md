Done! I've created both files:

**sumEven.js** — A function that sums all even numbers in an array using `reduce` with a check for `num % 2 === 0`.

**sumEven.test.js** — 7 Jest tests covering edge cases:
- Empty array
- Only even numbers
- Only odd numbers
- Mixed numbers
- Zero handling
- Negative even numbers

All 7 tests pass ✓
