Done! I've created both files:

**sumEven.js** — The function filters for even numbers (using `n % 2 === 0`) and reduces them to their sum.

**sumEven.test.js** — Five test cases covering:
- Mixed odd/even numbers
- Empty array
- No even numbers
- Negative numbers
- All even numbers
