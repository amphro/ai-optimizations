Done! I've created both files with all tests passing:

**sumEven.js** — The function uses `reduce()` to filter and sum even numbers (numbers where `n % 2 === 0`).

**sumEven.test.js** — 8 comprehensive tests covering:
- Empty arrays
- Arrays with no even numbers  
- Arrays with only even numbers
- Mixed arrays
- Negative even numbers
- Zero
- Single values (even and odd)

All tests pass ✓
