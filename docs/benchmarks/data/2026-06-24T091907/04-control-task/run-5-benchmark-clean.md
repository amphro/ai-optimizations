Done! I've created both files:

- **sumEven.js**: Contains the `sumEven` function that uses `reduce()` to sum all even numbers in the input array, exported via `module.exports`
- **sumEven.test.js**: Contains 6 Jest tests covering edge cases including empty arrays, no even numbers, all even numbers, negative evens, and zero

The function uses the modulo operator (`%`) to identify even numbers and accumulates them in the sum.
