Done! I've created both files:

**sumEven.js** — A function that uses `reduce` to filter even numbers and sum them.

**sumEven.test.js** — Jest tests covering 9 scenarios: mixed arrays, empty arrays, all odd/even numbers, negative numbers, zero, and single-element arrays.

The tests are ready to run with `npm test` or `jest sumEven.test.js`.
