Created `CLAUDE.md` at `/app/CLAUDE.md`. It covers:

- **Project & stack** — Node.js e-commerce API on PostgreSQL with GitHub Actions
- **Common commands** — install, dev, test, lint, migrate (flagged to adjust to your actual `package.json`)
- **Database** — migration-first workflow and parameterized queries
- **Testing** — test-on-change expectations
- **Commits** — Conventional Commits format with type list and examples
- **Deployment** — GitHub Actions pipeline and secrets handling
- **Conventions** — input validation, error shapes, layering

A couple of spots (script names, workflow paths) are written to match common defaults — worth tweaking if your repo names things differently.
