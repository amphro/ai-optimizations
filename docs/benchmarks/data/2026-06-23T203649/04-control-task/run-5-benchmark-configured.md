All 6 tests pass. Here's a summary of what was created:

**`sumEven.js`** — filters for even numbers (`n % 2 === 0`) then reduces to a sum, defaulting to `0` for an empty result.

**`sumEven.test.js`** — 6 tests covering:
- Mixed array of odds and evens
- Empty array → `0`
- All-odd array → `0`
- All-even array
- Negative even numbers
- Zero (which is even)
