Done. Both files are created:

- **`sumEven.js`** — filters even numbers and sums them with `reduce`, exported via `module.exports = { sumEven }`.
- **`sumEven.test.js`** — Jest tests covering mixed arrays, empty arrays, no-even cases, all-even, negatives, and zero.

To run the tests: `npx jest sumEven.test.js` (assuming Jest is installed).
