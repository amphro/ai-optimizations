All 5 tests pass. Created:

- **`sumEven.js`** — filters even numbers via `% 2 === 0` then reduces to a sum, exported via `module.exports`
- **`sumEven.test.js`** — 5 cases covering empty array, mixed numbers, all-odd, all-even, and negatives
- **`package.json`** — minimal Jest setup to run the tests
