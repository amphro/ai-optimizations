All 6 tests pass. Here's what was created:

**`sumEven.js`** — filters to even numbers and reduces to their sum. Returns `0` for an empty array via the `reduce` initial value.

**`sumEven.test.js`** — 6 cases covering:
- Mixed even/odd numbers
- Empty array → `0`
- All odd → `0`
- All even
- Negative even numbers
- Zero (even, included in sum)
