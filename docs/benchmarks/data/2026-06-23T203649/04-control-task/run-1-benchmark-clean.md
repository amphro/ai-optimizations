Done. Two files created:

- **`sumEven.js`** — `sumEven(numbers)` reduces the array, adding each value where `n % 2 === 0`.
- **`sumEven.test.js`** — Jest tests covering mixed arrays, empty arrays, no-evens, all-evens, negatives, and zero.

To run the tests: `npx jest sumEven.test.js` (requires Jest installed).
