All 6 tests pass. Here's a summary of what was created:

**`sumEven.js`** — filters for even numbers using `% 2 === 0`, then reduces to a sum (returns `0` for empty arrays via the reduce initial value).

**`sumEven.test.js`** — 6 tests covering: mixed numbers, empty array, all-odd, all-even, negative evens, and zero.
