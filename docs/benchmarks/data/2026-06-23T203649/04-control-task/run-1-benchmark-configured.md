All 6 tests pass. Here's what was created:

- **`sumEven.js`** — filters for even numbers (`n % 2 === 0`) then reduces to a sum, defaulting to `0` for empty arrays.
- **`sumEven.test.js`** — 6 cases covering: mixed numbers, empty array, no evens, all evens, negatives, and zero.
